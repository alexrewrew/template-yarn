@import '../../utilities/vars'
@import '../../utilities/mixins'

table, tr, th, td
  border 1px solid $black

table
  width 100%
  margin-bottom $r15
  caption
    caption-side top
    text-align right
  th
    padding $r5
  td
    padding $r5
