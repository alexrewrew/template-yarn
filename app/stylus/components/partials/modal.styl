@import '../../utilities/vars'
@import '../../utilities/mixins'

.fancybox-slide > div
  width 100%
  max-width 1170px
  +tab()
    max-width 960px
  +port()
    max-width 720px
  +fab()
    max-width 540px
  +mob()
    max-width calc(100% - 30px)

.fancybox-close-small
  &:hover
    &:after
      background $trans
      color lightblue
  &:focus
    &:after
      outline none
  &:after
    trans()
    color blue